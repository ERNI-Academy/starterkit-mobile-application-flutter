steps:
  - task: PowerShell@2
    inputs:
      targetType: filePath
      arguments: $(APP_ENVIRONMENT)
      filePath: "scripts/set_secrets_as_dart_define_params.ps1"
      workingDirectory: $(WorkingDirectory)
    displayName: Set `SecretsDartDefines` values